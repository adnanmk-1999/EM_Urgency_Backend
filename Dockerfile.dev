# Use Node 18 (stable and compatible with Sequelize projects)
FROM node:18

# Create working directory
WORKDIR /app

# Copy package.json and package-lock.json first
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the entire backend code
COPY . .

# Expose backend port (your backend listens on port in .env)
EXPOSE ${PORT}

# Start the backend
CMD ["npm", "start"]